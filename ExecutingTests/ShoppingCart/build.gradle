plugins {
    id 'ata-curriculum.snippets-conventions'
    id 'ata-curriculum.integration-test-conventions'
}

checkstyle {
    def suppressionFile = rootProject.file('Utilities/ATACheckstyle/configuration/checkstyle/unit1/suppressions.xml')
    configProperties.put('checkstyle.suppression.filter', suppressionFile)
}

def wflowToTestsMap = [
        'executingtests-writingcases-test': [
                'com.kenzie.executingtests.writingcases.WritingCasesTest'
        ],
        'executingtests-shoppingcart-test': [
                'com.kenzie.executingtests.shoppingcart.ShoppingCartTest'
        ],
        'executingtests-plantuml-test':[
                'com.kenzie.executingtests.plantuml.PlantUMLTest'
        ]
]

wflowToTestsMap.forEach { wflowName, testList ->
    tasks.register(wflowName) {
        dependsOn wflowtest
        test.doFirst {
            filter {
                testList.each { includeTestsMatching it }
            }
        }
    }
}
